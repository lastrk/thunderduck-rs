{
  "description": "Witness cases for tasks/select-block-review-findings.md — each pins one confirmed finding and is born red; flipping green is the acceptance signal for that finding's fix. Extended in the same commit whenever a cycle adds witnesses (F9, F10, ...). The F8/F10/F11 qualified-resolution cluster (filt-018/019, join-023, jn-024) was previously deferred pending ADR-023 ordinal-based qualified resolution; that behaviour LANDED via ADR-024 tier-3e (Pass F3, 2026-07-13). All 14 witnesses are now GREEN (verified 2026-07-15 via witness-progress.sh: 14/14 red->PASSED, 0 regressions) and none remain deferred; they stay in select_block_corpus_baseline.txt as the green regression oracle.",
  "witnesses": [
    { "case": "join-018", "finding": "F1", "pins": "drop over USING join — hoisted-order overwrite" },
    { "case": "join-020", "finding": "F2", "pins": "nested USING join side re-wrapped — hoisted-slot drop" },
    { "case": "cx-015",   "finding": "F3", "pins": "LATERAL VIEW over range() — stale default projections" },
    { "case": "cx-016",   "finding": "F3", "pins": "LATERAL VIEW over join — stale default projections" },
    { "case": "join-019", "finding": "F4", "pins": "multi-slot star over USING join (DataFrame)" },
    { "case": "jn-023",   "finding": "F4", "pins": "multi-slot star over USING join (SQL)" },
    { "case": "join-021", "finding": "F5", "pins": "user alias buried under USING parent — vis-exemption strand" },
    { "case": "join-022", "finding": "F7", "pins": "duplicate __td_jr synthetic-alias collision" },
    { "case": "agg-025",  "finding": "F9", "pins": "groupBy alias-qualified key over limit — aggregate wrap-path strand" },
    { "case": "proj-016", "finding": "F12", "pins": "relation-qualified star over limit-wrapped block — star wrap-boundary strand" },
    { "case": "filt-019", "finding": "F8",  "pins": "dead-alias qualifying a select-created alias ERRORS (Spark UNRESOLVED_COLUMN.WITH_SUGGESTION) — landed via ADR-024 tier-3e (Pass F3)" },
    { "case": "filt-018", "finding": "F10", "pins": "dead-alias projected-through column, double filter SUCCEEDS (Spark empty result) — landed via ADR-024 tier-3e (Pass F3)" },
    { "case": "join-023", "finding": "F11", "pins": "duplicate user alias on both join sides ERRORS (Spark AMBIGUOUS_REFERENCE), DataFrame — landed via ADR-024 tier-3e (Pass F3)" },
    { "case": "jn-024",   "finding": "F11", "pins": "duplicate alias both join sides ERRORS (Spark AMBIGUOUS_REFERENCE), SQL — landed via ADR-024 tier-3e (Pass F3)" }
  ]
}
